# Backend API Base URL. 
# Leave blank in development to proxy requests through Vite to avoid CORS issues.
# On Vercel, set VITE_API_BASE_URL in Vercel Environment Variables or it will default to https://api.lokonomy.in
VITE_API_BASE_URL=https://api.lokonomy.in
